@media $media-phone {
#video {
  transform:none !important;

  .logo-block ul {
    display:none !important;
  }
}

.logos .iwonder-link {
  padding-top:0;
  margin-left:0;
}

.logos .se-link {
  margin-right:0;
}

.logo {
  margin-top:50px;
}
  body {
    background:black;
  }
  .float-left-mobile {
    float:left;
    width:auto;
  }

 .pad-bottom-mobile {
    padding-bottom:1em;
  }

  .hide-mobile {
    display:none;
  }

  .credits .text-right {
    font-style:italic;
    color:#d6c8c9;
    text-align:center;

  }

  #about .container {
    margin-left:20px;
    margin-right:20px;
  }

  #about .row-padded-top-and-bottom {
    padding-bottom:10px;
  }
  .credits .text-large {
    text-align:center;
  }

// .credits .text-centre {
//   text-align:left;
// }

  .pad-top-mobile {
    // margin-top: 1em;
  }

  .layout-video {
    // display:none !important;
  }

  .controls-wrap {
    display:none;
  }

  .row-padded-top-and-bottom {
    padding-top:10px;
    padding-bottom:30px;
  }

  .emphasis {
    font-size:24px; line-height:25px;
    padding-top:0.4em;
    padding-bottom:0.4em;
    text-align:center;
  }

  .subheading {
    // font-size: $font-size-medium;
  }

  .text-small {
        font-size: $font-size-small-mobile;
        line-height: ($line-height-base + 0.3);
    }
    .text-medium {
        font-size: $font-size-medium-mobile;

    }
    .text-medium-plus {
        font-size: $font-size-medium-plus-mobile;
    }

    .text-large {
        font-size: $font-size-large-mobile;
    }

  .mobile-only {
    display:block;

  }

  img.footballers-logo {
    width: 47%;

}


.buffering-video {
  display:none !important;
}

  .toggle-notifications, .toggle-close, .toggle-close-message, .toggle-open-message, .quiet-time {
    display:none !important;
  }

  .video-section-nav {
    position:relative;

    span {
      position:absolute;
      top:5px;
      left:5px;
      width:20px; height:20px;

      &.next {
        left:auto;
        right:5px;
      }
    }

    em {
      font-style:normal;
    }

  &.first span.previous {
    display:none;
  }

  &.last span.next {
    display:none;
  }

  }


  .mobile-title-box {
    background:$pink;
    padding-top:4px;
  }
  .menu-bar {
    position:fixed;
    top:0; width:80px; right:0;
    height:$menu-height;

    background:transparent;
    z-index:400;

    &.open {
      background:$pink;
      width:100%;
      height:auto;
      overflow:visible;
      transition:background 0.3s;
      .mobile-nav {
        opacity:1;
        height:auto;

      }
    }

  .mobile-nav {
    opacity:0;
    height:0px;
    overflow:hidden;
    transition:opacity 0.3s;
    transition-delay:0.3s;
    clear:both;
  }
    ul {

      display:block;

      li {
        display:block;
        float:none;
        text-align:center;

        a {
          @extends $heading;
          font-size: $heading-size-medium;
           text-transform:uppercase;
           padding:10px 0 7px;
           border-top:1px solid $purple;
        }

      }
    }

  }

  .subheading {
    padding-left:10%;
    padding-right:10%;
  }

  .icon-menu {
    font-size:2.4em;
    float:right;
    margin-right:10px;

  }
  /***** HOME PAGE ******/

     .intro-description {
      text-align:center;
    }


  .home-logo {
    position:static;
      top:-20px;
      background:rgba($pink,0.6);
      // padding:5px 0;


    .logo-wrap, .logo-wrap-section {
      position:absolute;
      top:10px;
      left: 0; right: 0;
      text-align:center;
    }
    .logo-wrap-section {
      width:70%;
    }
    img {

    }
  }

  .chapter-controls .video-controls {
    width:auto;
  }

header.section-header {
  background:$purple;
  // padding-bottom:10px;
  min-height:150px;


  width:100%;
  overflow:hidden;
}

  .header .section-masthead {
    position:absolute;
    top:0; left:0; width:100%;
    height:auto;
    display:none;
  }

  .carousel {
    position:relative;
    top:0; left:0; bottom:auto; right:auto;
    //overflow:visible;
    min-height:180px;

    background:black;
  width:500%;
  //overflow:hidden;
  overflow-y:hidden;
    display:block;
   // width:100%;

transition:margin-left 0.3s;
  &.pos1 {

  }

  &.pos2 {
    margin-left:-100%;
  }
  &.pos3 {
    margin-left:-200%;
  }

  &.pos4 {
    margin-left:-300%;
  }

  &.pos5 {
    margin-left:-400%;
  }
    .section {
      width:20%;
      height:auto;
      bottom:auto;
      display:block;
      position:relative;
      float:left;

      left:0 !important;

      .info {
        top:35%;

        readable-shadow(rgba(0,0,0,0.3));
      }

      .tint {
        display:none;
      }
       &.current {
        left:0%;
      }

      &.previous {
        left:-100%;
      }

      &:after {
        right:-30px;
        font-size:30px;
           z-index:100;
      }

      &.next {
        left:100%;

        &:after {
          right:auto;
          left:-30px;


        }
      }



       &.left2, &.left3, &.left4 {
      left:-200%;
    }


    &.right2, &.right3, &.right4, &.right5 {
      left:200%;
    }


    }
  }


  .gallery-overlay {
    .image-holder {
      bottom:50%;
    }

    .info-holder.shown {
      top:50%;
    }
  }
.content-grid .box .background{
  position:relative;
}
  .content-grid .box {
    padding-top:0;
    height:auto;
    position:relative;
  min-height:0;
    .foreground {
      position:absolute;
      top:80px;
      left:0; right:0;
    }
  }

  .content-grid .col8 .box .foreground {
    top:40px;
  }


  .social {
    padding:20px;
  }
  #facebook-overlay {
    bottom:0;
    background:#371c29;

    .bar-column , .count-column, .label-column {
      text-align:center !important;
    }

    .stat-container {
      width:auto;
      margin-top:20px;
      .bar {
        width:80%;
        float:none;
        margin:0 auto 10px;
      }

      .label-column  {
        margin-bottom:20px;
      }

      .after {
        /* Be VERY careful with this, breaks normal words wh_erever */
     word-break: break-all;

     /* Non standard for webkit */
     word-break: break-word;

-webkit-hyphens: auto;
   -moz-hyphens: auto;
        hyphens: auto;
      }
    }
  }

  .section.current .bg-container {
    animation: none !important;
  }

  .social {
    margin-top:20px; margin-bottom:20px;
  }

  .icon-control-large {
        height:30px; width: 30px;


    }


  .structure-wrap {
    display:none !important;
  }
    /*** Video ***/
    .mejs-container  {
      margin-top:50px !important;
    }

    #video.in-archive {
      display:none !important;
    }

    #video .logo-block {
      top:10px; left:10px; width:80px;
      //z-index:1000;
    }


    .archive-holder {
      // display:none;
      background:$purple;
      position:absolute;
      left:0; right:0;
      overflow:auto;
      -webkit-overflow-scrolling: touch;

        li {
          border-bottom:1px solid rgba(white,0.3);
          padding:10px;

          .type-icon {
            float:left;
            margin-top:6px;
          }

          .subheading {
            padding-left:0;
            margin-left:25px;
          }

          .heading-medium {
            font-size:20px;
            line-height:21px;
          }
        }
    }

    /*** ARCHIVE ***/

     #archive .logo-block .mini-logo {
    width:80px;
    margin-top:0;
  }

  #archive .logo-block {
    top:10px; left:10px;

  }

  #archive .logo-block img {
    position:absolute;
    top:0; left:0;
    transition:left 0.3s;
    transition-delay:0.3s;
  }

  #archive .logo-block.move-in img {
    transition-delay:0;
     left:25px;
  }

  #archive .close {
    display:none;
  }

  .archive-item .meta {
    display:block;
    position:relative;
    opacity:1;
    transform:none !important;
    top:0; left:0; right:0; width:100%;
    overflow: hidden;
    border-bottom:1px solid rgba(white,0.3);
    padding:1em 6px 1em;
    margin-bottom:1em;
    margin-left:0;
    padding-top:1em !important;

    .date {
      margin:0.3em 0;
    }

  }
  .archive-item.scrolled-past .meta {
    opacity:1;
  }
  .back-to-video {
    font-family: $subtitle-font-family;

    font-weight: normal;
    text-transform: uppercase;
    font-size: $font-size-large;
    float:left;
    width:10px;
    opacity:0;
    transition-delay:0s;
    transition:opacity 0.3s;
    visibility:hidden;
  }

  .move-in .back-to-video {
    opacity:1;
    transition-delay:0.3s;
    visibility:visible;
  }


  #archive .close {
    right:60px;
  }

  #archive .archive-items {
    padding:10px;
    position:absolute;
    border-top:1px solid rgba(white,0.3);
    top:50px; bottom:0;
    left:0; right:0;
    overflow:auto;
    -webkit-overflow-scrolling: touch;

  }



    .archive-item {
      display:none !important;
      border-bottom:none;
      padding-bottom:0;
      &.scrolled-to {
        display:block !important;
      }

  .text-large {
    font-size:20px;
    line-height:21px;
  }
      .archive-content {
        padding-bottom:0;
      }
    }

}

@media $media-big-phone {
  .carousel {
      min-height:220px;

      .background {
        margin-top:-30px;
      }
    }
  }